{
  "name": "pmconnect-server",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "bun build ./src/server.ts --outdir ./build --target bun",
    "start:dev": "bun --hot ./src/server.ts",
    "start:prod": "bun ./build/server.js",
    "stripe:forward": "stripe listen --forward-to localhost:3000/api/stripe/webhooks"
  },
  "license": "MIT",
  "dependencies": {
    "@types/parse": "^3.0.9",
    "express": "^4.18.2",
    "parse": "^4.3.1",
    "stripe": "^14.8.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.9.0",
    "typescript": "^5.2.2"
  }
}
